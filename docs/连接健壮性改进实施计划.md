# 连接健壮性改进 — 详细实施计划

> 基于《连接健壮性改进方案》，按依赖顺序拆分为 4 步，每步包含具体改动和代码。

---

## 前置说明

**日志格式规范**：所有新增日志统一为：
```typescript
console.error('[pi-to-chrome] <位置> <简述>', error);
```

**测试方式**：每步完成后无需专门测试，正常使用即可观察日志输出。断线感知和健康检查在后续步骤中一起验证。

---

## 第一步：消灭空 catch（4 个文件，纯日志，无行为变更）

### 1.1 `core/browser.ts` — `getActivePage()` 两个空 catch

**外层 catch**（`b.pages()` 失败）：

```typescript
// 改前
  } catch {}

  throw new Error('无法获取当前页面');

// 改后
  } catch (error) {
    console.error('[pi-to-chrome] getActivePage: b.pages() 失败', error);
  }

  throw new Error('无法获取当前页面');
```

**内层 catch**（`page.evaluate()` 失败）：

```typescript
// 改前
      try {
        const visibilityState = await page.evaluate(() => document.visibilityState);
        if (visibilityState === 'visible') {
          return page;
        }
      } catch {}

// 改后
      try {
        const visibilityState = await page.evaluate(() => document.visibilityState);
        if (visibilityState === 'visible') {
          return page;
        }
      } catch (error) {
        console.error(`[pi-to-chrome] getActivePage: page.evaluate() 失败 (url=${page.url()})`, error);
      }
```

### 1.2 `tools/inspect-styles.ts` — CDP session detach 的空 catch

```typescript
// 改前
      if (cdpSession) {
        try {
          await cdpSession.detach();
        } catch {}
      }

// 改后
      if (cdpSession) {
        try {
          await cdpSession.detach();
        } catch (error) {
          console.error('[pi-to-chrome] inspect-styles: cdpSession.detach() 失败', error);
        }
      }
```

### 1.3 `tools/take-screenshot.ts` — viewport 恢复的空 catch

```typescript
// 改前
      if (originalViewport) {
        try {
          await page.setViewport(originalViewport);
        } catch {}
      }

// 改后
      if (originalViewport) {
        try {
          await page.setViewport(originalViewport);
        } catch (error) {
          console.error('[pi-to-chrome] take-screenshot: viewport 恢复失败', error);
        }
      }
```

### 1.4 `console-buffer.ts` — 三个空 catch

**① `startListening` 中 `browser.pages()` 的 catch：**

```typescript
// 改前
    browser.pages().then(pages => {
      for (const page of pages) {
        this.attachPageListener(page);
      }
    }).catch(() => {});

// 改后
    browser.pages().then(pages => {
      for (const page of pages) {
        this.attachPageListener(page);
      }
    }).catch((error) => {
      console.error('[pi-to-chrome] console-buffer: browser.pages() 失败', error);
    });
```

**② `targetcreated` 中 `page()` 的 catch：**

```typescript
// 改前
    browser.on('targetcreated', async (target) => {
      if (target.type() === 'page') {
        const page = await target.page();
        if (page) {
          this.attachPageListener(page);
        }
      }
    });

// 改后
    browser.on('targetcreated', async (target) => {
      if (target.type() === 'page') {
        try {
          const page = await target.page();
          if (page) {
            this.attachPageListener(page);
          }
        } catch (error) {
          console.error('[pi-to-chrome] console-buffer: targetcreated page() 失败', error);
        }
      }
    });
```

**③ `stopListening` 中 `page.off` 的 catch：**

```typescript
// 改前
      try {
        page.off('console', handler);
      } catch {}

// 改后
      try {
        page.off('console', handler);
      } catch (error) {
        console.error('[pi-to-chrome] console-buffer: page.off() 失败', error);
      }
```

### 1.5 顺手补漏：`core/browser.ts` — `disconnectChrome()` 的空 catch

```typescript
// 改前
  if (browser) {
    try {
      browser.disconnect();
    } catch {}
    browser = null;
  }

// 改后
  if (browser) {
    try {
      browser.disconnect();
    } catch (error) {
      console.error('[pi-to-chrome] disconnectChrome: browser.disconnect() 失败', error);
    }
    browser = null;
  }
```

---

## 第二步：断线感知 + ensureConnection（`core/browser.ts` 一个文件）

### 2.1 新增模块状态

在文件顶部的模块状态区域新增：

```typescript
// ─── 断线回调 ───
let disconnectedCallback: (() => void) | null = null;

// ─── 健康检查缓存 ───
let lastHealthCheckTime = 0;
let lastHealthCheckResult = true;
const HEALTH_CHECK_TTL_MS = 5000; // 5 秒内有效
```

### 2.2 新增 `onDisconnected()` 注册接口

```typescript
/** 注册断线回调（由 index.ts 调用，回调中无 pi ctx，只有 pi 可用） */
export function onDisconnected(cb: () => void): void {
  disconnectedCallback = cb;
}
```

### 2.3 新增 `ensureConnection()` 方法

```typescript
/** 健康检查：确认 browser 连接有效。失败则 throw 明确错误。 */
export async function ensureConnection(): Promise<void> {
  if (!browser) {
    throw new Error('Chrome 连接已断开，请执行 /chrome-start 重连');
  }

  // 利用缓存避免并发工具调用时重复 CDP 请求
  const now = Date.now();
  if (now - lastHealthCheckTime < HEALTH_CHECK_TTL_MS && lastHealthCheckResult) {
    return;
  }

  try {
    await browser.version();
    lastHealthCheckTime = now;
    lastHealthCheckResult = true;
  } catch (error) {
    lastHealthCheckResult = false;
    console.error('[pi-to-chrome] ensureConnection: browser.version() 失败', error);
    throw new Error('Chrome 连接已断开，请执行 /chrome-start 重连');
  }
}
```

### 2.4 修改 `connectChrome()` — 连接成功后注册断线监听

```typescript
// 改前
export async function connectChrome(): Promise<Browser> {
  const puppeteer = await import('puppeteer-core');
  browser = await puppeteer.connect({ browserURL: getDebugUrl(), defaultViewport: null });
  return browser;
}

// 改后
export async function connectChrome(): Promise<Browser> {
  const puppeteer = await import('puppeteer-core');
  browser = await puppeteer.connect({ browserURL: getDebugUrl(), defaultViewport: null });

  // 注册断线监听
  browser.on('disconnected', () => {
    console.error('[pi-to-chrome] browser disconnected 事件触发');
    browser = null;                          // 直接置 null，不做任何 CDP 请求
    lastHealthCheckResult = false;           // 失效化健康检查缓存
    disconnectedCallback?.();                // 通知 index.ts 做清理和通知
  });

  return browser;
}
```

### 2.5 修改 `disconnectChrome()` — 主动断开时清理状态

```typescript
// 改后（在已有的 catch 日志改动基础上）
export async function disconnectChrome(): Promise<void> {
  if (browser) {
    try {
      browser.disconnect();
    } catch (error) {
      console.error('[pi-to-chrome] disconnectChrome: browser.disconnect() 失败', error);
    }
    browser = null;
    lastHealthCheckResult = false;
  }
}
```

注意：主动调用 `disconnectChrome()` 时，puppeteer 会触发 `disconnected` 事件，回调会再次将 `browser` 置 `null`——这是幂等的，不会出问题。但 `disconnectedCallback?.()` 会被调用，所以 `index.ts` 中的守卫 `if (toolNames.length === 0) return;` 很关键。

---

## 第三步：`tool-registry.ts` 加 ensureConnection

### 3.1 新增 import

```typescript
// 改前
import { getActivePage } from './core/browser';

// 改后
import { ensureConnection, getActivePage } from './core/browser';
```

### 3.2 execute 入口加前置检查

```typescript
// 改前
    async execute(toolCallId, params, signal, onUpdate, ctx) {
      const page = await getActivePage();
      return tool.execute(page, params, { consoleBuffer });
    }

// 改后
    async execute(toolCallId, params, signal, onUpdate, ctx) {
      await ensureConnection();
      const page = await getActivePage();
      return tool.execute(page, params, { consoleBuffer });
    }
```

---

## 第四步：`index.ts` 注册断线回调

### 4.1 新增 import

```typescript
// 在已有的 browser import 中加入 onDisconnected：
import * as browser from './core/browser';
//（不需要改，* as browser 已经包含）
```

### 4.2 在 `connectChrome()` 成功后注册断线回调

在 `chrome-start` 命令的 handler 中，`consoleBuffer.startListening(...)` 之后添加：

```typescript
        consoleBuffer.startListening(browser.getBrowser());

        // 注册断线回调（仅注册一次即可，重连时会覆盖）
        browser.onDisconnected(() => {
          if (toolNames.length === 0) return;

          consoleBuffer.stopListening();
          const allActive = pi.getActiveTools();
          const remaining = allActive.filter((name: string) => !toolNames.includes(name));
          pi.setActiveTools(remaining);
          toolNames = [];

          pi.sendMessage({
            customType: 'chrome-disconnected',
            content: '⚠️ Chrome 连接已断开！\n如需查看原因，请立即切换到 Chrome 查看 console 日志\n如需重新连接，请执行 /chrome-start',
            display: true,
          });
        });

        toolNames = registerTools(pi, consoleBuffer);
```

### 4.3 `chrome-stop` 中主动断开前先移除回调（可选但更清晰）

```typescript
// 在 /chrome-stop 的实际断开操作之前，先清空 toolNames
// 这样 disconnectChrome() 触发 disconnected 事件时，回调中的守卫会直接 return
// 现有代码已经是先清 toolNames 再 disconnectChrome()，顺序正确，无需额外改动
```

---

## 改动汇总

| 步骤 | 文件 | 改动量 |
|------|------|--------|
| 1 | `core/browser.ts` | 3 处空 catch 加日志 |
| 1 | `tools/inspect-styles.ts` | 1 处空 catch 加日志 |
| 1 | `tools/take-screenshot.ts` | 1 处空 catch 加日志 |
| 1 | `console-buffer.ts` | 3 处空 catch 加日志 |
| 2 | `core/browser.ts` | 新增状态 + `onDisconnected()` + `ensureConnection()` + 修改 `connectChrome()` / `disconnectChrome()` |
| 3 | `tool-registry.ts` | 加 import + 1 行 `ensureConnection()` 调用 |
| 4 | `index.ts` | 加 `onDisconnected()` 回调注册（~12 行） |

总计：**5 个文件**，核心逻辑集中在 `core/browser.ts`。

---

## 验证方式

1. **第一步验证**：正常使用，观察 `console.error` 是否在出错时输出（需要在 pi 启动终端查看，或通过 `read-console` 工具查看）
2. **第二步验证**：连接 Chrome 后手动关闭 Chrome，观察是否出现断线通知
3. **第三步验证**：断线后尝试调用任何 Chrome 工具，确认报错为"连接已断开"而非"无法获取当前页面"
4. **第四步验证**：断线后执行 `/chrome-start` 重连，确认工具重新注册可用
